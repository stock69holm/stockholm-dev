import React from "react";
import Contact from "./Contact";

const ContactSection = () => (
    <section>
        <h2 style={{ textAlign: "center", marginTop: "30px", color: "#ffffff" }}>Контакты</h2>
        <Contact />
    </section>
);

export default ContactSection;